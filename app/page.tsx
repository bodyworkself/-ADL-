// 既存の巨大な画面は app/prognosis-prediction/page.tsx にあります。
// ここではそれをトップページから読み込んで出すだけ。
import PrognosisPage from './prognosis-prediction/page'

export default function Page() {
  return <PrognosisPage />
}
